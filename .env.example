# Database connection string
# If database is in another docker container in the same network:
# DATABASE_URL=postgresql://user:password@container_name:5432/dbname
DATABASE_URL=postgresql://postgres:password@localhost:5432/vote_manager

# Secret key for JWT tokens
JWT_SECRET=generate-a-strong-secret-key-here

# Application port (inside container)
PORT=5000
